require "spec_helper"

describe ColoradoHikes::Hike do
  context '.new_from_page' do
    it 'it instantiates a mtn based on the colorado best hikes webpage' do
      #X = Mountain.new_from_page("http://thecohiker.com/colorado-hiking-trails/")

      #expect(#peak).to be_an_instance_of(Mountain)
      #expect(x.name).to eq("X")
      #expect(x.standard_route).to eq("X)
      #expect(x.elevation_gain).to eq("")
      #expect(x.distance).to eq("")
      #expect(x.klass_difficulty).to eq("")

    end
  end

  content 'details' do
end

describe ColoradoHikes::Hike do
  context 'properties' do
    it 'has a name'
    it ...
  end
end
